diff --git a/Config.h b/Config.h
--- a/Config.h
+++ b/Config.h
@@ -65,2 +65,2 @@
-#define AXIS1_LIMIT_MIN              -180 //   -180, n. Where n= -90..-360 (degrees.) Minimum "Hour Angle" or Azimuth.        Adjust
-#define AXIS1_LIMIT_MAX               180 //    180, n. Where n=  90.. 360 (degrees.) Maximum "Hour Angle" or Azimuth.        Adjust
+#define AXIS1_LIMIT_MIN              -120 //   -180, 比赛初始软限位；必须按实机无碰撞范围复核。                                Adjust
+#define AXIS1_LIMIT_MAX               120 //    180, 比赛初始软限位；必须按实机无碰撞范围复核。                                Adjust
@@ -84 +84,3 @@
-#define AXIS1_SENSE_HOME              OFF //    OFF, HIGH or LOW enables & state clockwise home position, as seen from front. Option
+#define AXIS1_SENSE_HOME             HIGH //    HIGH: AUX3/GPIO14 常闭触点打开时触发。                                         Option
+#define AXIS1_SENSE_HOME_INIT INPUT_PULLUP //    常闭开关接 GPIO14 与 GND；禁止外接 5V/12V。                                  Option
+#define AXIS1_SENSE_HOME_DIST_LIMIT 100.0 //     限制自动回零最大搜索角，首次测试前确认方向。                                  Adjust
@@ -100,2 +102,2 @@
-#define AXIS2_LIMIT_MIN               -90 //    -90, n. Where n=-90..0 (degrees.) Minimum allowed Declination or Altitude.    Infreq
-#define AXIS2_LIMIT_MAX                90 //     90, n. Where n=0..90 (degrees.) Maximum allowed Declination or Altitude.     Infreq
+#define AXIS2_LIMIT_MIN               -90 //    -90, GEM 回零位位于赤纬极点，不可简单收窄到 -85。                             Infreq
+#define AXIS2_LIMIT_MAX                90 //     90, 轴2实际无碰撞范围必须在卸载相机后单独验证。                               Infreq
@@ -119 +121,3 @@
-#define AXIS2_SENSE_HOME              OFF //    OFF, HIGH or LOW enables & state clockwise home position, as seen from above. Option
+#define AXIS2_SENSE_HOME             HIGH //    HIGH: AUX4/GPIO13 常闭触点打开时触发。                                         Option
+#define AXIS2_SENSE_HOME_INIT INPUT_PULLUP //    常闭开关接 GPIO13 与 GND；禁止外接 5V/12V。                                  Option
+#define AXIS2_SENSE_HOME_DIST_LIMIT 100.0 //     限制自动回零最大搜索角，首次测试前确认方向。                                  Adjust
@@ -169 +173 @@
-#define LIMIT_STRICT                  OFF //    OFF, disables limits until unpark goto or sync. ON enables limits at startup. Option
+#define LIMIT_STRICT                   ON //     ON, 未设置日期时间前禁止电机动作，并从启动开始执行软限位。                     Option
@@ -194 +198 @@
-#define SLEW_RATE_BASE_DESIRED        1.0 //    1.0, n. Desired slew rate in deg/sec. Adjustable at run-time from            <-Req'd
+#define SLEW_RATE_BASE_DESIRED        0.5 //    1.0, 首次比赛联调限速；确认负载、刹车距离后再逐步调整。                         <-Req'd
diff --git a/Extended.config.h b/Extended.config.h
--- a/Extended.config.h
+++ b/Extended.config.h
@@ -40,4 +40,16 @@
-#define SERIAL_BT_MODE               SLAVE //    OFF, Use SLAVE to enable the interface (ESP32 only.)                          Option
+#define SERIAL_BT_MODE                 OFF //    关闭经典蓝牙；ESP32 不能同时启用蓝牙串口和 IP 串口。                           Option
 #define SERIAL_BT_NAME          "LX200" //         "OnStepX", Bluetooth device name.                                        Adjust
-#define SERIAL_IP_MODE                OFF //    OFF, WIFI_ACCESS_POINT or WIFI_STATION enables interface (ESP32 only.)        Option
+#define SERIAL_IP_MODE       WIFI_STATION //    接入树莓派建立的 StarScope 局域网。                                           Option
 #define WEB_SERVER                    OFF //    OFF, ON enables Webserver (for Website plugin)                                Option
+
+// StarFinding 比赛网络。提交代码前不要把现场真实密码写入 Git。
+#define SERIAL_SERVER            STANDARD //    标准 LX200 TCP 命令通道固定监听 9999 端口。
+#define MDNS_SERVER                    ON //    Pad 可优先发现 starfinding-mount.local。
+#define MDNS_NAME     "starfinding-mount"
+#define STA_AUTO_RECONNECT            true
+#define STA_SSID               "StarScope"
+#define STA_PASSWORD "REPLACE_WITH_A_STRONG_PASSWORD"
+#define STA_DHCP_ENABLED              true //    由树莓派 DHCP 分配；在 NetworkManager 中按 MAC 保留 10.42.0.20。
+
+// 不启用 AP fallback，避免现场误连赤道仪热点后与相机网关失去通信。
+#define STA_AP_FALLBACK               false
